<%= form_with(model: molecule, local: true) do |form| %>
  <% if molecule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(molecule.errors.count, "error") %> prohibited this molecule from being saved:</h2>

      <ul>
      <% molecule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :molecule_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
