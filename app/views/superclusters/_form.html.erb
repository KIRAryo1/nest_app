<%= form_with(model: supercluster, local: true) do |form| %>
  <% if supercluster.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supercluster.errors.count, "error") %> prohibited this supercluster from being saved:</h2>

      <ul>
      <% supercluster.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :supercluster_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
